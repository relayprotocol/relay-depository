[features]
seeds = false
skip-lint = false

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[programs.localnet]
my_program = "Hz17BWNjrZXrVDjQxYXZGwhdCAQakmMBS5EcZfZxxvto"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
test-forwarder = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/relay-forwarder.ts"
test-escrow = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/relay-escrow.ts"
